// Prettier formats a template tagged html and no other, which is all this is
// for. It escapes nothing, so what a template interpolates is markup already,
// and never a name or a figure that came from outside the desk.
const html = String.raw

export default html
